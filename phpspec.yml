suites:
    main:
        namespace: Karriere\State
        src_path: src
        spec_prefix: spec

extensions:
  LeanPHP\PhpSpec\CodeCoverage\CodeCoverageExtension:
    format:
      - html
      - clover
    output:
      html: coverage
      clover: coverage.xml

formatter.name: pretty
